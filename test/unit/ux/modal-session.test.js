import sandboxed from '../../helpers/dom-sandbox';
import ModalSession from '/ux/modal-session';

xdescribe('ModalSession', sandboxed(function(sand) {
	it('catches all events and manages a modal session', () => {
		let modalSession = new ModalSession();
	});
}));
