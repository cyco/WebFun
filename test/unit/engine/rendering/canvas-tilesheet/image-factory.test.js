import ImageFactory from "src/engine/rendering/canvas-tilesheet/image-factory";

describe("WebFun.Unit.Engine.Rendering.CanvasTilesheet.ImageFactory", () => {
	it("must still be written", () => {});
});
